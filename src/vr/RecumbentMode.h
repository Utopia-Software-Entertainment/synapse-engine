#pragma once

namespace synapse {

class RecumbentMode
{
public:
    RecumbentMode() = default;
    ~RecumbentMode() = default;

    RecumbentMode(const RecumbentMode&) = delete;
    RecumbentMode& operator=(const RecumbentMode&) = delete;
};

} // namespace synapse
